services:
  skills-harness:
    image: ghcr.io/arisecrewyes/skills:latest
    container_name: skills-harness
    restart: unless-stopped
    expose:
      - "9100"
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - root_default

networks:
  root_default:
    external: true
